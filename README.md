# Solspire Pages

Static GitHub Pages landing page for Solspire multiplayer invitations.

The URL fragment contains a versioned `solspire:` Join Code. The page never
receives or stores it server-side; an explicit Full or Demo button passes it to
the selected Steam application as `+connect_code`.
